server:
  port: 8083

spring:
  application:
    name: course-enrollment-service
  datasource:
    url: jdbc:postgresql://localhost:15432/academic_db
    username: artacademy
    password: artacademy123
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    locations: classpath:db/migration

artacademy:
  user:
    # Default initial auth password assigned to newly created users when the UI supplies none.
    # Users are forced to reset it on first login.
    default-temporary-password: ChangeMe@123
